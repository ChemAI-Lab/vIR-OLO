# Example dataset YAML for YOLO (modify paths & names accordingly)
# Root path to dataset (can be relative to this file)
# Absolute path used to prevent Ultralytics from prefixing with its global datasets dir.
path: D:/Garcilazo/Python/00Exercises/CHEMAI_projects/00_Qichen/YOLO_detection/dataset

# Train/val/test image directories (relative to 'path')
train: images/train
val: images/val
# test: images/test  # optional

# Number of classes (redundant if names dict length matches)
nc: 12

# Class names mapping (index: name)
names:
  0: label0
  1: label1
  2: label2
